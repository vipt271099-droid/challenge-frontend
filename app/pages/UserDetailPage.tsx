import React from "react";
import UserDetail from "../routes/user.$id";

// Page wrapper to align with the /pages/UserDetailPage structure.
export default function UserDetailPage() {
  return <UserDetail />;
}
